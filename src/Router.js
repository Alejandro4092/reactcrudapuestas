import React, { Component } from 'react'
import axios

export default class Router extends Component {
  render() {
    return (
     <BrowserRouter>
        <MenuRutas />

        <Routes>
          
        </Routes>
      </BrowserRouter>
    )
  }
}
